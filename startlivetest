#!/usr/bin/env bash
# Start KalshiBaby using config.production.yaml (live account, test configuration)
set -euo pipefail
cd "$(dirname "$0")"
export KALSHI_CONFIG=config.production.yaml
exec .venv/bin/uvicorn kalshibaby_backend:app --host 0.0.0.0 --port 8765 --reload
